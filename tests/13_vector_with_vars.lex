// Vectors with Var elements (rather than Const literals). Each prior Var
// must be registered in MnemoTable so it dispatches as Var-atr when reused
// inside the vector.
X=10
Y=20
Z=30
V#X,Y,Z
W=99
